export enum FilterAction {
  remove = 'remove',
  update = 'update',
};

export const enum FilterType {
  endDate = 'endDate',
  includeArchived = 'includeArchived',
  startDate = 'startDate',
  status = 'status',
  tags = 'tags',
};
